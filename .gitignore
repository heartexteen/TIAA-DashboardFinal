node_modules/
.next/
.DS_Store

# Real sponsor / client data — never commit. See data-access.md.
# Note: pdfData/ and *.pdf are intentionally NOT ignored — the PDFs currently
# in this repo are synthetic test personas (Carina, John Smith, Maria),
# not real client data. If real sponsor data is ever shared, add rules here.
data/
*.csv
*.xlsx

# Local env + build artifacts
.env
.env.local
.env*.local
tsconfig.tsbuildinfo
